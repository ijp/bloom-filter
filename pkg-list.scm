(package (bloom-filter (0 1))
  (synopsis "Bloom Filter in Scheme")
  (description
   "A quick and dirty bloom filter to show off guile's bitvectors.")
  (homepage "http://github.com/ijp/bloom-filter")
  (libraries "bloom-filter.scm"))
